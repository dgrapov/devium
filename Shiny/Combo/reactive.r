# # need to source from inside Server
	# output$variable<-renderUI({
			# var.opts<-colnames(ui.opts$data())#colnames(get(input$data))#
			# selectInput("variable", "Y-variable", var.opts)
			# # updateSelectInput(session, "variable", choices = var.opts)
		 # })
	
	# output$group<-renderUI({
			# var.opts<-colnames(ui.opts$data())#colnames(get(input$data))#
			# selectInput("group", "X-variable", var.opts)
			# # updateSelectInput(session, "variable", choices = var.opts)
		 # })